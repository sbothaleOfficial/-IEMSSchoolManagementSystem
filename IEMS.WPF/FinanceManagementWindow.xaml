<Window x:Class="IEMS.WPF.FinanceManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Finance Management" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Height="80" Padding="20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="btnBack" Content="← Back"
                            Click="BtnBack_Click"
                            Background="White" Foreground="Black" BorderBrush="#CCCCCC" BorderThickness="1"
                            Width="80" Height="32" Margin="0,0,20,0" FontWeight="SemiBold"/>
                    <TextBlock Text="Finance Management &amp; Analytics"
                               FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource TextOnPrimaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Academic Year:" Foreground="{StaticResource TextOnPrimaryBrush}" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cmbAcademicYear" Width="120" Height="30"
                              SelectionChanged="CmbAcademicYear_SelectionChanged"/>
                    <Button x:Name="btnRefreshAnalytics" Content="🔄 Refresh"
                            Click="BtnRefreshAnalytics_Click"
                            Background="White" Foreground="Black" BorderBrush="#CCCCCC" BorderThickness="1"
                            Width="80" Height="30" Margin="10,0,0,0" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tab Control -->
        <TabControl Grid.Row="1" Margin="10">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource BorderBrush}"
                                        BorderThickness="1,1,1,0"
                                        CornerRadius="4,4,0,0"
                                        Margin="2,0">
                                    <ContentPresenter x:Name="ContentSite"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"
                                                    ContentSource="Header"
                                                    Margin="12,8"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextOnPrimaryBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </TabControl.Resources>


            <!-- Expense Management Tab -->
            <!-- Electricity Bills Tab -->
            <TabItem Header="🔌 Electricity Bills">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Action Buttons Row -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <Button x:Name="btnAddElectricityBill" Content="Add Bill" Width="120" Height="35" Margin="0,0,10,0"
                                Click="BtnAddElectricityBill_Click" Style="{StaticResource SuccessButtonStyle}"/>
                        <Button x:Name="btnEditElectricityBill" Content="Edit Bill" Width="120" Height="35" Margin="0,0,10,0"
                                Click="BtnEditElectricityBill_Click" Style="{StaticResource PrimaryButtonStyle}"/>
                        <Button x:Name="btnDeleteElectricityBill" Content="Delete Bill" Width="130" Height="35" Margin="0,0,10,0"
                                Click="BtnDeleteElectricityBill_Click" Style="{StaticResource DangerButtonStyle}"/>
                    </StackPanel>

                    <!-- Electricity Bills Header -->
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Electricity Bills Management" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="Track monthly electricity bills, units consumed and payment status" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>

                    <!-- Electricity Bills DataGrid -->
                    <DataGrid Grid.Row="2" x:Name="dgElectricityBills"
                              AutoGenerateColumns="False" CanUserAddRows="False"
                              MouseDoubleClick="DgElectricityBills_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Bill Number" Binding="{Binding BillNumber}" Width="120"/>
                            <DataGridTextColumn Header="Month/Year" Binding="{Binding MonthYearDisplay}" Width="120"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=₹{0:N2}}" Width="100"/>
                            <DataGridTextColumn Header="Units" Binding="{Binding Units}" Width="80"/>
                            <DataGridTextColumn Header="Rate/Unit" Binding="{Binding UnitsRate, StringFormat=₹{0:N2}}" Width="90"/>
                            <DataGridTextColumn Header="Due Date" Binding="{Binding FormattedDueDate}" Width="100"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding PaymentStatus}" Width="80"/>
                            <DataGridTextColumn Header="Paid Date" Binding="{Binding FormattedPaidDate}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Other Expenses Tab -->
            <TabItem Header="📝 Other Expenses">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Action Buttons Row -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <Button x:Name="btnAddOtherExpense" Content="Add Expense" Width="120" Height="35" Margin="0,0,10,0"
                                Click="BtnAddOtherExpense_Click" Style="{StaticResource SuccessButtonStyle}"/>
                        <Button x:Name="btnEditOtherExpense" Content="Edit Expense" Width="130" Height="35" Margin="0,0,10,0"
                                Click="BtnEditOtherExpense_Click" Style="{StaticResource PrimaryButtonStyle}"/>
                        <Button x:Name="btnDeleteOtherExpense" Content="Delete Expense" Width="140" Height="35" Margin="0,0,10,0"
                                Click="BtnDeleteOtherExpense_Click" Style="{StaticResource DangerButtonStyle}"/>
                        <Button x:Name="btnClearFilter" Content="Clear Filter" Width="100" Height="35" Margin="10,0,0,0"
                                Click="BtnClearFilter_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                    </StackPanel>

                    <!-- Other Expenses Header -->
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Other Expenses Management" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="Track stationery, events, maintenance and other school-related expenses" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>

                    <!-- Category Filter -->
                    <Grid Grid.Row="2" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Filter by Category:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <ComboBox Grid.Column="1" x:Name="cmbCategoryFilter" SelectionChanged="CmbCategoryFilter_SelectionChanged"/>
                    </Grid>

                    <!-- Other Expenses DataGrid -->
                    <DataGrid Grid.Row="3" x:Name="dgOtherExpenses"
                              AutoGenerateColumns="False" CanUserAddRows="False"
                              MouseDoubleClick="DgOtherExpenses_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryDisplay}" Width="120"/>
                            <DataGridTextColumn Header="Expense Type" Binding="{Binding ExpenseType}" Width="150"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=₹{0:N2}}" Width="100"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding FormattedExpenseDate}" Width="100"/>
                            <DataGridTextColumn Header="Vendor" Binding="{Binding VendorName}" Width="120"/>
                            <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Expense Analytics Tab -->
            <TabItem Header="📊 Expense Analytics">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>  <!-- Dashboard Header -->
                            <RowDefinition Height="Auto"/>  <!-- View Selection -->
                            <RowDefinition Height="Auto"/>  <!-- Financial Overview Title -->
                            <RowDefinition Height="Auto"/>  <!-- Summary Cards -->
                            <RowDefinition Height="Auto"/>  <!-- Expense Categories Title -->
                            <RowDefinition Height="Auto"/>  <!-- Detailed Expense Cards -->
                            <RowDefinition Height="Auto"/>  <!-- Financial Balance Title -->
                            <RowDefinition Height="Auto"/>  <!-- Total Income vs Expenses -->
                            <RowDefinition Height="Auto"/>  <!-- Detailed Analytics Title -->
                            <RowDefinition Height="*"/>     <!-- Detailed Analytics Grid -->
                        </Grid.RowDefinitions>

                        <!-- Dashboard Header -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Expense Analytics Dashboard" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock Text="Comprehensive view of all school expenses including transport, fees, salaries and utilities" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                        </StackPanel>

                        <!-- View Selection -->
                        <Grid Grid.Row="1" Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Text="View:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimaryBrush}"/>
                                <RadioButton x:Name="rbMonthly" Content="Monthly" Margin="0,0,15,0"
                                             Checked="ViewRadioButton_Checked"/>
                                <RadioButton x:Name="rbYearly" Content="Yearly" Margin="0,0,15,0"
                                             Checked="ViewRadioButton_Checked"/>
                                <RadioButton x:Name="rbOverall" Content="Overall" IsChecked="True"
                                             Checked="ViewRadioButton_Checked"/>
                            </StackPanel>

                        </Grid>

                        <!-- Key Financial Summary Cards -->
                        <TextBlock Grid.Row="2" Text="📈 Financial Overview" FontSize="14" FontWeight="Bold"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <UniformGrid Grid.Row="3" Columns="3" Margin="0,0,0,20">
                            <!-- Student Fees Collection Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#4CAF50" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="💰" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Fees Collected" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtFeesCollected" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#4CAF50"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Pending Fees Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#F44336" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="⚠️" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Pending Fees" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtPendingFees" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#F44336"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Total Expenses Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#FF5722" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="💸" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Total Expenses" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtAllExpensesTotal" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#FF5722"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <!-- Detailed Expense Categories -->
                        <TextBlock Grid.Row="4" Text="💼 Expense Categories" FontSize="14" FontWeight="Bold"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,20,0,10"/>
                        <UniformGrid Grid.Row="5" Columns="4" Margin="0,0,0,20">
                            <!-- Electricity Expenses Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#FF9800" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="⚡" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Electricity" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtElectricityTotal" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#FF9800"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Other Expenses Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#9C27B0" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="📝" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Other Expenses" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtOtherExpensesTotal" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#9C27B0"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Transport Expenses Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#2196F3" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="🚌" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Transport" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtTransportTotal" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#2196F3"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Staff Salaries Card -->
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="5"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="#4CAF50" Width="50" Height="50" CornerRadius="25" Margin="0,0,10,0">
                                            <TextBlock Text="👥" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Staff Salaries" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                            <TextBlock x:Name="txtSalariesTotal" Text="₹0.00" FontSize="18" FontWeight="Bold" Foreground="#4CAF50"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <!-- Total Income vs Expenses Summary -->
                        <TextBlock Grid.Row="6" Text="⚖️ Financial Balance" FontSize="14" FontWeight="Bold"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,20,0,10"/>
                        <Border Grid.Row="7" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,20"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="Fees Collected" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtTotalIncome" Text="₹0.00" FontSize="24" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBlock Text="(Student Fees)" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="Total Expenses" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtTotalExpenses" Text="₹0.00" FontSize="24" FontWeight="Bold" Foreground="#F44336" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBlock Text="(All Categories)" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="Net Profit/Loss" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="txtNetBalance" Text="₹0.00" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                    <TextBlock Text="(Fees - Expenses)" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Detailed Analytics and Reports -->
                        <TextBlock Grid.Row="8" Text="📊 Detailed Analytics" FontSize="14" FontWeight="Bold"
                                  Foreground="{StaticResource TextPrimaryBrush}" Margin="0,20,0,10"/>
                        <Grid Grid.Row="9">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Fee Collection Breakdown -->
                            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="0,0,10,0"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Fee Collection Breakdown" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                    <DataGrid x:Name="dgFeeBreakdown" AutoGenerateColumns="False" CanUserAddRows="False"
                                              Height="250">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Fee Type" Binding="{Binding FeeType}" Width="*"/>
                                            <DataGridTextColumn Header="Expected" Binding="{Binding ExpectedAmount, StringFormat=₹{0:N2}}" Width="85"/>
                                            <DataGridTextColumn Header="Collected" Binding="{Binding CollectedAmount, StringFormat=₹{0:N2}}" Width="85"/>
                                            <DataGridTextColumn Header="Pending" Binding="{Binding PendingAmount, StringFormat=₹{0:N2}}" Width="85"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Border>

                            <!-- Expense Category Breakdown -->
                            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15" Margin="10,0,0,0"
                                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Expense Category Breakdown" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                    <DataGrid x:Name="dgCategoryBreakdown" AutoGenerateColumns="False" CanUserAddRows="False"
                                              Height="250">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*"/>
                                            <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=₹{0:N2}}" Width="120"/>
                                            <DataGridTextColumn Header="%" Binding="{Binding Percentage, StringFormat={}{0:F1}%}" Width="60"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Height="25" Background="{StaticResource SecondaryDarkBrush}">
            <StatusBarItem>
                <TextBlock x:Name="lblStatus" Text="Ready" Foreground="{StaticResource TextOnPrimaryBrush}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>