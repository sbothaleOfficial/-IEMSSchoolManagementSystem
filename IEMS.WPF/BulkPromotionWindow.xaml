<Window x:Class="IEMS.WPF.BulkPromotionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bulk Student Promotion" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">

    <!-- Removed local style definitions - using global styles from App.xaml -->

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Background="{StaticResource PrimaryBrush}">
            <StackPanel>
                <TextBlock Text="Bulk Student Promotion" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextOnPrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Promote multiple students from one class to another" FontSize="12" Foreground="{StaticResource PrimaryLightBrush}" HorizontalAlignment="Center" Margin="0,5"/>
            </StackPanel>
        </Border>

        <!-- Promotion Configuration -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="From Class" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtFromClass" Height="35" TextChanged="TxtFromClass_TextChanged"
                             ToolTip="Enter class name (e.g., Nursery, KG1, KG2, Class 1, Class 2, etc.)"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0">
                    <TextBlock Text="To Class" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtToClass" Height="35" TextChanged="TxtToClass_TextChanged"
                             ToolTip="Enter class name (e.g., Nursery, KG1, KG2, Class 1, Class 2, etc.)"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10,0,0,0">
                    <TextBlock Text="Academic Year" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cmbAcademicYear" Height="35" DisplayMemberPath="Year" SelectedValuePath="Year"/>
                </StackPanel>

                <StackPanel Grid.Column="3" VerticalAlignment="Bottom" Margin="15,0,0,0">
                    <Button x:Name="btnLoadPreview" Content="Load Preview" Height="35" Width="120"
                            Style="{StaticResource PrimaryButtonStyle}" Click="BtnLoadPreview_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="2" Margin="0,10">
            <!-- Preview Tab -->
            <TabItem Header="Preview Students">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Summary Cards -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <Border Style="{StaticResource CardStyle}" Background="{StaticResource SuccessLightBrush}" MinWidth="150">
                            <StackPanel>
                                <TextBlock Text="Total Students" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="txtTotalStudents" Text="0" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Background="{StaticResource PrimaryLightBrush}" MinWidth="150">
                            <StackPanel>
                                <TextBlock Text="Eligible" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="txtEligibleStudents" Text="0" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Background="{StaticResource WarningLightBrush}" MinWidth="150">
                            <StackPanel>
                                <TextBlock Text="Excluded" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="txtExcludedStudents" Text="0" FontSize="20" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Students DataGrid -->
                    <DataGrid Grid.Row="1" x:Name="dgPromotionPreview" AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False" GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column" SelectionMode="Extended">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="Exclude" Width="70">
                                <DataGridCheckBoxColumn.Binding>
                                    <Binding Path="IsExcluded" UpdateSourceTrigger="PropertyChanged"/>
                                </DataGridCheckBoxColumn.Binding>
                            </DataGridCheckBoxColumn>
                            <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Student No." Binding="{Binding StudentNumber}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Current Class" Binding="{Binding CurrentClass}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Target Class" Binding="{Binding TargetClass}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Pending Fees" Binding="{Binding PendingAmount, StringFormat=₹{0:N2}}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Eligibility" Binding="{Binding IneligibilityReason}" Width="200" IsReadOnly="True"/>
                            <DataGridCheckBoxColumn Header="Eligible" Binding="{Binding IsEligible}" Width="80" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Filter Options -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                        <CheckBox x:Name="chkShowOnlyEligible" Content="Show only eligible students" Margin="0,0,20,0" Checked="ChkShowOnlyEligible_Checked" Unchecked="ChkShowOnlyEligible_Unchecked"/>
                        <CheckBox x:Name="chkShowPendingFees" Content="Show students with pending fees" Checked="ChkShowPendingFees_Checked" Unchecked="ChkShowPendingFees_Unchecked"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Results Tab -->
            <TabItem Header="Promotion Results" x:Name="tabResults">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Results Summary -->
                    <Border Grid.Row="0" Style="{StaticResource CardStyle}" x:Name="borderResultsSummary" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="txtResultsSummary" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock x:Name="txtPromotionDate" FontSize="12" Foreground="Gray" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Errors DataGrid -->
                    <DataGrid Grid.Row="1" x:Name="dgPromotionErrors" AutoGenerateColumns="False"
                              CanUserAddRows="False" CanUserDeleteRows="False" GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Student Name" Binding="{Binding StudentName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Error Description" Binding="{Binding Error}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnExecutePromotion" Content="Execute Promotion" Width="150" Height="40"
                    Style="{StaticResource SuccessButtonStyle}" Click="BtnExecutePromotion_Click" IsEnabled="False"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="100" Height="40" Margin="10,0,0,0"
                    Style="{StaticResource WarningButtonStyle}" Click="BtnCancel_Click"/>
        </StackPanel>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" VerticalAlignment="Bottom" Margin="0,50,0,-25">
            <TextBlock x:Name="lblStatus" Text="Ready" FontSize="12"/>
        </StatusBar>
    </Grid>
</Window>