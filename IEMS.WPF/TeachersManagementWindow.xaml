<Window x:Class="IEMS.WPF.TeachersManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teachers Management" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Background="#9B59B6" Orientation="Horizontal" Height="60">
            <TextBlock Text="👨‍🏫" FontSize="26" Margin="20,0" VerticalAlignment="Center"/>
            <TextBlock Text="Teachers Management System"
                       FontSize="20" FontWeight="Bold" Foreground="White"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,10,0,20">
                <Button x:Name="btnAddTeacher" Content="Add Teacher" Width="100" Height="35" Margin="0,0,10,0"
                        Click="BtnAddTeacher_Click" Background="#2ECC71" Foreground="White" BorderThickness="0"/>
                <Button x:Name="btnEditTeacher" Content="Edit Teacher" Width="100" Height="35" Margin="0,0,10,0"
                        Click="BtnEditTeacher_Click" Background="#F39C12" Foreground="White" BorderThickness="0"/>
                <Button x:Name="btnDeleteTeacher" Content="Delete Teacher" Width="100" Height="35" Margin="0,0,10,0"
                        Click="BtnDeleteTeacher_Click" Background="#E74C3C" Foreground="White" BorderThickness="0"/>
                <Button x:Name="btnRefreshTeachers" Content="Refresh" Width="100" Height="35" Margin="0,0,10,0"
                        Click="BtnRefreshTeachers_Click" Background="#95A5A6" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <DataGrid x:Name="dgTeachers" Grid.Row="1" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      GridLinesVisibility="All" HeadersVisibility="Column" AlternatingRowBackground="#F8F9FA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId}" Width="100"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Subject" Binding="{Binding Subject}" Width="150"/>
                    <DataGridTextColumn Header="Classes" Binding="{Binding ClassCount}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Height="25" Background="#34495E">
            <StatusBarItem>
                <TextBlock x:Name="lblStatus" Text="Ready" Foreground="White"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>