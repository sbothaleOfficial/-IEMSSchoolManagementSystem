<Window x:Class="IEMS.WPF.FeeStructureManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IEMS.WPF"
        mc:Ignorable="d"
        Title="Fee Structure Management - IEMS" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <local:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" BasedOn="{StaticResource ActionButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" BasedOn="{StaticResource ActionButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="White" Padding="20" Margin="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Direction="270" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Fee Structure Management" Style="{StaticResource HeaderTextStyle}"/>
                    <TextBlock Text="Configure fee amounts for different classes and fee types"
                             Style="{StaticResource SubHeaderTextStyle}" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Name="btnAddFeeStructure" Content="➕ Add Fee Structure"
                          Style="{StaticResource ActionButtonStyle}"
                          Margin="0,0,10,0" Click="BtnAddFeeStructure_Click"/>
                    <Button Name="btnRefresh" Content="🔄 Refresh"
                          Style="{StaticResource SecondaryButtonStyle}"
                          Width="110" Height="35"
                          Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filter Section -->
        <Border Grid.Row="1" Background="White" Margin="0,0,0,1" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Academic Year Filter -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Academic Year" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Name="cmbAcademicYear"
                            SelectionChanged="CmbAcademicYear_SelectionChanged"/>
                </StackPanel>

                <!-- Class Filter -->
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Class" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Name="cmbClassFilter"
                            SelectionChanged="CmbClassFilter_SelectionChanged"/>
                </StackPanel>

                <!-- Fee Type Filter -->
                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="Fee Type" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Name="cmbFeeTypeFilter"
                            SelectionChanged="CmbFeeTypeFilter_SelectionChanged"/>
                </StackPanel>

                <!-- Status Filter -->
                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="Status" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox Name="cmbStatusFilter"
                            SelectionChanged="CmbStatusFilter_SelectionChanged">
                        <ComboBoxItem Content="All" IsSelected="True"/>
                        <ComboBoxItem Content="Active"/>
                        <ComboBoxItem Content="Inactive"/>
                    </ComboBox>
                </StackPanel>

                <!-- Search Box -->
                <StackPanel Grid.Column="4" Margin="0,0,10,0">
                    <TextBlock Text="Search" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Name="txtSearch"
                           TextChanged="TxtSearch_TextChanged"
                           Tag="Search by description..."/>
                </StackPanel>

                <!-- Clear Filters Button -->
                <Button Grid.Column="5" Name="btnClearFilters"
                      Content="Clear Filters"
                      Style="{StaticResource SecondaryButtonStyle}"
                      VerticalAlignment="Bottom"
                      Click="BtnClearFilters_Click"/>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="White" Margin="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" Direction="270" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DataGrid Name="dgFeeStructures"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        AlternatingRowBackground="{StaticResource SurfaceBrush}"
                        BorderThickness="0"
                        SelectionMode="Single"
                        MouseDoubleClick="DgFeeStructures_MouseDoubleClick">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="45"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource SelectedRowBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Academic Year" Binding="{Binding AcademicYear}" Width="120"/>
                        <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" Width="150"/>
                        <DataGridTextColumn Header="Fee Type" Binding="{Binding FeeType}" Width="120"/>
                        <DataGridTextColumn Header="Amount (₹)" Binding="{Binding Amount, StringFormat='{}{0:N2}'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="Padding" Value="0,0,10,0"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        <DataGridTemplateColumn Header="Status" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10"
                                          Padding="8,4"
                                          HorizontalAlignment="Center"
                                          Background="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}">
                                        <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}"
                                                 Foreground="White"
                                                 FontSize="11"
                                                 FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️ Edit"
                                              Tag="{Binding Id}"
                                              Style="{StaticResource ActionButtonStyle}"
                                              Height="30"
                                              Margin="0,0,5,0"
                                              Click="BtnEdit_Click"/>
                                        <Button Content="🗑️ Delete"
                                              Tag="{Binding Id}"
                                              Style="{StaticResource DangerButtonStyle}"
                                              Height="30"
                                              Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty State -->
                <Border Name="emptyStatePanel"
                      Visibility="Collapsed"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                        <TextBlock Text="No Fee Structures Found"
                                 FontSize="18"
                                 FontWeight="SemiBold"
                                 Foreground="{StaticResource SecondaryTextBrush}"
                                 HorizontalAlignment="Center"
                                 Margin="0,10,0,5"/>
                        <TextBlock Text="Add fee structures to configure fees for classes"
                                 Foreground="{StaticResource SecondaryTextBrush}"
                                 HorizontalAlignment="Center"/>
                        <Button Content="Add First Fee Structure"
                              Style="{StaticResource ActionButtonStyle}"
                              HorizontalAlignment="Center"
                              Margin="0,20,0,0"
                              Click="BtnAddFeeStructure_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="{StaticResource PrimaryBrush}">
            <StatusBarItem>
                <TextBlock Name="lblStatus" Foreground="White" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Name="lblTotalRecords" Foreground="White" Text="Total Records: 0"/>
                    <Separator Width="20" Background="Transparent"/>
                    <TextBlock Name="lblTotalAmount" Foreground="White" Text="Total Amount: ₹0.00" Margin="0,0,10,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>